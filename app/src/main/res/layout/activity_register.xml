<?xml version="1.0" encoding="utf-8"?>
<!--
    RegisterActivity — contenedor de los 3 Fragments.
    Solo tiene: toolbar con back + dots de progreso + FragmentContainerView.
    Todos los botones y contenido viven dentro de los Fragments.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/registerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/windowBackground"
    tools:context=".auth.RegisterActivity">

    <!-- ── Toolbar ── -->
    <LinearLayout
        android:id="@+id/registerToolbar"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp">

        <!-- Botón atrás -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Atrás"
            app:tint="?attr/colorOnBackground"
            android:visibility="invisible" />

        <!-- Spacer -->
        <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1" />

        <!-- Dots de progreso — 4 pasos -->
        <LinearLayout
            android:id="@+id/dotsContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <View
                android:id="@+id/dot1"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/bg_step_dot_active" />

            <View
                android:id="@+id/dot2"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/bg_step_dot_inactive" />

            <View
                android:id="@+id/dot3"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/bg_step_dot_inactive" />

            <View
                android:id="@+id/dot4"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/bg_step_dot_inactive" />

            <View
                android:id="@+id/dot5"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/bg_step_dot_inactive" />
        </LinearLayout>

        <!-- Spacer derecho para equilibrar -->
        <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1" />

        <!-- Placeholder del mismo tamaño que btnBack para centrar los dots -->
        <Space android:layout_width="40dp" android:layout_height="40dp" />

    </LinearLayout>

    <!-- ── Contenedor de Fragments ── -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/registerFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
